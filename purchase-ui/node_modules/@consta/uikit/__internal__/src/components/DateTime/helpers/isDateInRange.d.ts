import { DateRange } from '../../../utils/types/Date';
declare type IsDateInRangeResult = 'first' | 'last' | boolean;
export declare const isDateInRange: (date: Date, range: DateRange, startOf?: ((date: Date) => Date) | undefined) => IsDateInRangeResult;
export {};
