import React from 'react';
import { ResponsesImageProps } from "../../responsesImages/ResponsesImage/ResponsesImage";
import { ResponsesProps } from './Responses';
declare type CreateResponsesArguments = {
    name: string;
    image: React.FC<ResponsesImageProps>;
    title?: React.ReactNode;
    description?: React.ReactNode;
    actions?: React.ReactNode;
};
export declare function createResponses({ name, image, title, description, actions, }: CreateResponsesArguments): React.ForwardRefExoticComponent<Omit<ResponsesProps, "image"> & React.RefAttributes<HTMLDivElement>>;
export {};
