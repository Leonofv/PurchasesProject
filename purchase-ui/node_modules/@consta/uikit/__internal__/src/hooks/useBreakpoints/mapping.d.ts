import { Map, Returned } from './types';
export declare const sortObj: <T extends {}>(obj: T) => T;
export declare const mapping: <POINTS extends string>(width: number, map: Map<POINTS>) => Returned<POINTS>;
