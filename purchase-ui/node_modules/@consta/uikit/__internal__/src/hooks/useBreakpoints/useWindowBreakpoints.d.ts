import { Map, Returned } from './types';
export declare const useWindowBreakpoints: <POINTS extends string>({ isActive, map, }: {
    isActive?: boolean | undefined;
    map: Map<POINTS>;
}) => Returned<POINTS>;
